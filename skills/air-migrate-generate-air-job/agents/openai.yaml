interface:
  display_name: "AIR Migrate: Generate Job"
  short_description: "Build AIR code from vetted templates"
  default_prompt: "Use $air-migrate-generate-air-job to generate and preflight the selected AIR workload without crossing the configured execution gate."
