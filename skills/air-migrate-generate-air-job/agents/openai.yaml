interface:
  display_name: "AIR Migrate: Generate Job"
  short_description: "Build a source-aware AIR handoff"
  default_prompt: "Use $air-migrate-generate-air-job to enforce peft_only, generate the planned Volume, system.ai, or Hugging Face AIR workload, validate it locally, and stop before execution."
