interface:
  display_name: "AIR Migrate: Validate Model"
  short_description: "Compare legacy and migrated models"
  default_prompt: "Use $air-migrate-validate-model-migration to smoke-test an AI Runtime workload and compare its model with the legacy source."
