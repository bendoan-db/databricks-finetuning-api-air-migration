interface:
  display_name: "AIR Migrate: Plan Training"
  short_description: "Design an AI Runtime migration plan"
  default_prompt: "Use $air-migrate-plan-air-training to create an AI Runtime training plan that preserves the configured compute and execution gate."
