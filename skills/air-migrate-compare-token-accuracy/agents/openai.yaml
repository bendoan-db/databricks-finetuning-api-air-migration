interface:
  display_name: "AIR Migrate: Retired Token Accuracy"
  short_description: "Historical evaluation compatibility only"
  default_prompt: "This skill is retired from the generation-only migration workflow."
