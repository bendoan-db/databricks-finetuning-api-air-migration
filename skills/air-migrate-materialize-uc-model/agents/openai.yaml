interface:
  display_name: "AIR Migrate: Load UC Weights"
  short_description: "Resolve and stage AIR training weights"
  default_prompt: "Use $air-migrate-materialize-uc-model to apply use_existing_weights, resolve the training source, and stage any selected Unity Catalog weights."
