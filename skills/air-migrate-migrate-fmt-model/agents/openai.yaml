interface:
  display_name: "AIR Migrate: Orchestrate FMT Model"
  short_description: "Generate an FMT-to-AIR workload handoff"
  default_prompt: "Use $air-migrate-migrate-fmt-model to apply peft_only, generate the configured AIR workload, and stop before training, registration, or evaluation."
