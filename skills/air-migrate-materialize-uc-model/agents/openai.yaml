interface:
  display_name: "AIR Migrate: Resolve UC Weights"
  short_description: "Reuse Volume weights or stage them through AIR"
  default_prompt: "Use $air-migrate-materialize-uc-model to resolve input weights through a configured Volume or AIR while preserving the migration execution gate."
